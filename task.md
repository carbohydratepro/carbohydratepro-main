# タスクリスト

<!--
  タスクは箇条書きで記載してください。
  /run_task を実行すると、一番上のタスクが実装されます。
  完了したタスクは自動的に done.md に移動されます。
-->
1. [ ] マイページ画面の改善
    - 各画面の設定をマイページ画面からは一括で行えるようにする
    - 問い合わせ領域を確保する

1. [ ] トップ画面の改修
    - 家計簿繊維ではなくマイページ遷移とする
    - 各画面の説明を追加する
    - 更新履歴は領域を狭め、スクロールで閲覧できるようにする
    - 更新履歴の主張が激しいのでシンプルにする
    - done.mdの内容から更新履歴を更新する（粒度は浅めでよい）

1. [ ] 一時タスク画面の改修
    - タブを閉じるとすべて消えてしまうため、非同期で保存する
    - 保存されていないものアイコンや背景色等でわかるようにする（主張は強くなくてよい）

1. [ ] 全体的な操作方法の改善
    - 編集ボタン、削除ボタンを削除し、ダブルタップ/クリックで編集。長押し→ゴミ箱アイコンで削除とするようにする。
    - 一時タスク画面では未着手、進行中、終了を領域でステータス変更としているが、やること/やってる/できたの領域が横並べになっている場合はその領域に移動でステータス変更とする。削除領域を薄赤で表示

1. [ ] pythonのlinter指摘を解消する

1. [ ] タスク管理画面のカレンダー連携
    - googleカレンダーやiphoneカレンダーと連携

1. 各画面の説明をiアイコンクリックで表示するようにする

1. [ ] 未ログインユーザー用のデモ画面実装
    - ユーザー作成/ログインしないと各画面に触ることができなく、新規ユーザーが増えにくい
    各画面のデモ画面等を用意し、ログインすることで全機能を触れるようにしてください。

1. [ ] 文言修正
    - 新規登録→収支登録

1. [ ] 全体的なUIの修正

1. [ ] トップ画面
    - ログイン中アクセス時、ログイン画面ではなくトップ画面に遷移するように修正

1. [ ] suica連携
    - 任意のsuicaを連携することができ、支払をそのまま支出登録する

1. [ ] クレジットカード連携
    - 任意のクレジットカードを連携することができ、支払をそのまま支出登録する
1. [ ] paypay連携
    - 任意のpaypayを連携することができ、支払をそのまま支出登録する

1. [ ] ログファイルの圧縮（S3転送）