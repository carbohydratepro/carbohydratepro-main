# リリース前 E2E 自動化仕様

`test.md` の確認項目のうち、まず Playwright に落とすスモークテストを定義します。未自動化の項目は `test.md` のチェックリストとして残し、必要に応じてこのファイルへケース化して追加します。

<a id="e2e-auth-001"></a>
## E2E-AUTH-001 ログインできる

- 対応元: `test.md` 1. 認証・アカウント
- 前提データ: `E2E_USER_EMAIL` と `E2E_USER_PASSWORD` で指定した有効ユーザーが存在する。
- 手順: `/login/` を開き、メールアドレスとパスワードを入力し、ログインボタンを押す。
- 期待値: `/carbohydratepro/home/` へ遷移し、統合ダッシュボードとログイン済みナビが表示される。
- 自動化: `e2e/tests/auth.spec.ts`
- 手動確認: メール認証状態、アカウント停止状態、複数ロールの差分。

<a id="e2e-auth-002"></a>
## E2E-AUTH-002 ログイン失敗時に分かりやすいエラーが表示される

- 対応元: `test.md` 1. 認証・アカウント
- 前提データ: なし。
- 手順: `/login/` を開き、存在しないメールアドレスと任意の誤ったパスワードでログインする。
- 期待値: `メールアドレスとパスワードが一致しません。` が表示され、ログイン画面に留まる。
- 自動化: `e2e/tests/auth.spec.ts`
- 手動確認: 未入力時のブラウザ/サーバー側必須エラー。

<a id="e2e-auth-003"></a>
## E2E-AUTH-003 ログアウト後に保護ページへ直接アクセスするとログイン画面へ遷移する

- 対応元: `test.md` 1. 認証・アカウント、17. セキュリティ
- 前提データ: `E2E_USER_EMAIL` と `E2E_USER_PASSWORD` で指定した有効ユーザーが存在する。
- 手順: ログインし、ログアウトし、`/carbohydratepro/expenses/` を直接開く。
- 期待値: `/login/?next=%2Fcarbohydratepro%2Fexpenses%2F` へ遷移し、ログイン画面が表示される。
- 自動化: `e2e/tests/auth.spec.ts`
- 手動確認: ブラウザバック、セッション固定、他ユーザーID指定。

<a id="e2e-auth-004"></a>
## E2E-AUTH-004 サインアップ時にパスワード不一致エラーが表示される

- 対応元: `test.md` 1. 認証・アカウント
- 前提データ: なし。テストごとに未使用のユーザー名とメールアドレスを生成する。
- 手順: `/signup/` を開き、ユーザー名、メールアドレス、異なる2つのパスワードを入力して送信する。
- 期待値: `/signup/` に留まり、確認用パスワード欄がエラー表示になり、パスワード不一致の文言が表示される。
- 自動化: `e2e/tests/auth.spec.ts`
- 手動確認: 登録成功時の確認メール、重複メール、弱いパスワード、メール認証完了フロー。

<a id="e2e-auth-005"></a>
## E2E-AUTH-005 未登録メールのパスワードリセット申請でも完了画面になる

- 対応元: `test.md` 1. 認証・アカウント、17. セキュリティ
- 前提データ: なし。
- 手順: `/password_reset/` を開き、存在しないメールアドレスを入力して送信する。
- 期待値: `/password_reset_done/` へ遷移し、登録有無を明かさない共通の完了文言が表示される。
- 自動化: `e2e/tests/auth.spec.ts`
- 手動確認: 登録済みメールへの実メール送信、リセットリンクの期限切れ、パスワード変更完了。

<a id="e2e-acc-001"></a>
## E2E-ACC-001 アカウントを追加して相互に切り替え、解除できる

- 対応元: `test.md` 1. 認証・アカウント（複数アカウント切替）
- 前提データ: `E2E_USER_EMAIL`/`E2E_USER_PASSWORD`（メイン）と `E2E_SECONDARY_USER_EMAIL`/`E2E_SECONDARY_USER_PASSWORD`（サブ）の2ユーザーが存在し、互いに未連携である。
- 手順: メインでログインし、ヘッダーのアカウントメニューから「アカウント追加」を開き、サブの認証情報で追加する。アカウント編集ページで現在アカウントを確認し、メインへパスワードなしで切り替える。最後にサブの連携を解除する。
- 期待値: 追加後はサブへ切り替わり「切り替えました」が表示される。編集ページに両アカウントが表示され、切替後はメインが「現在のアカウント」になる。解除後はサブが一覧から消える。
- 自動化: `e2e/tests/account-switch.spec.ts`
- 手動確認: アイコン画像アップロード、孫アカウント（サブの他の連携先）が一覧に露出しないことのUI確認。

<a id="e2e-acc-002"></a>
## E2E-ACC-002 現在のアカウントのみログアウトすると連携アカウントへ自動切替する

- 対応元: `test.md` 1. 認証・アカウント（複数アカウント切替）
- 前提データ: E2E-ACC-001 と同じ2ユーザー。
- 手順: メインでログインしてサブを追加（現在=サブ）。アカウントメニューの「ログアウト」を押す。編集ページで状態を確認後、サブを解除して完全にログアウトする。
- 期待値: サブのみログアウトされメインへ自動切替し、「現在のアカウントからログアウトし、〜に切り替えました」が表示される。編集ページでサブに「ログアウト済み」バッジが付く。最後のログアウトでログイン画面へ遷移する。
- 自動化: `e2e/tests/account-switch.spec.ts`
- 手動確認: ログアウト済みアカウントへの「ログインして切り替え」でメール入力済みログイン画面になること。

<a id="e2e-nav-001"></a>
## E2E-NAV-001 ログイン後の主要ナビから各画面へ遷移できる

- 対応元: `test.md` 2. トップ・共通ナビゲーション
- 前提データ: `E2E_USER_EMAIL` と `E2E_USER_PASSWORD` で指定した有効ユーザーが存在する。
- 手順: ログイン後、ナビから家計簿、スケジュール、一時タスク、習慣、メモ、買い物リスト、マイページを順に開く。
- 期待値: 各リンクの遷移先 URL とページ本文が対象機能に一致する。
- 自動化: `e2e/tests/navigation.spec.ts`
- 手動確認: 現在ページの視覚表示、スタッフ専用管理メニュー。

<a id="e2e-demo-001"></a>
## E2E-DEMO-001 デモトップから各デモ機能へ遷移できる

- 対応元: `test.md` 14. デモモード
- 前提データ: なし。
- 手順: `/demo/` を開き、家計簿、スケジュール、一時タスク、習慣、メモ、買い物リストの各デモ URL を確認する。
- 期待値: 各画面で `デモモード` が表示され、対象機能の本文が表示される。
- 自動化: `e2e/tests/demo.spec.ts`
- 手動確認: 閲覧専用であることの文言、登録/編集/削除ボタンの無効化または説明。

<a id="e2e-resp-001"></a>
## E2E-RESP-001 390px幅で主要導線を操作できる

- 対応元: `test.md` 15. スマホ・レスポンシブ
- 前提データ: なし。
- 手順: 390px 幅で `/top/` を開き、ハンバーガーメニューを開いてデモへ遷移する。
- 期待値: トップの見出し、ナビ内リンク、デモ画面が表示される。
- 自動化: `e2e/tests/responsive.spec.ts`
- 手動確認: 360px 幅、768px 幅、固定フッター被り、実機タップ/長押し/スワイプ。

<a id="e2e-resp-002"></a>
## E2E-RESP-002 390px幅でボトムタブから各画面へ遷移できる

- 対応元: UI刷新（サイドバー + モバイルボトムタブ）
- 前提データ: なし。
- 手順: 390px 幅で `/demo/home/` を開き、ボトムタブから家計簿へ、「その他」メニューからメモへ遷移する。
- 期待値: 各画面へ遷移でき、モバイルではサイドバーが表示されない。
- 自動化: `e2e/tests/responsive.spec.ts`
- 手動確認: 実機での safe-area（ホームインジケーター）との重なり、タブのタップ領域。

<a id="e2e-resp-003"></a>
## E2E-RESP-003 スマホの家計簿グラフを必要な項目だけ展開できる

- 対応元: 全画面スマートフォン表示改善。
- 前提データ: デモデータ。
- 手順: 390px幅でデモ家計簿を開き、「その他」メニューと「日別支出」の折りたたみを操作する。
- 期待値: 収支登録だけが主要操作として表示され、補助操作はメニューから利用できる。カテゴリグラフだけが初期展開され、閉じていた日別支出グラフが操作後に正しい幅で表示される。
- 自動化: `e2e/tests/responsive.spec.ts`

<a id="e2e-resp-004"></a>
## E2E-RESP-004 スマホの月カレンダーから予定一覧を開ける

- 対応元: 全画面スマートフォン表示改善。
- 前提データ: デモデータ。
- 手順: 390px幅でデモスケジュールの予定がある日付セルを確認し、タップする。
- 期待値: セル内は色印と件数だけが表示され、タップ後のモーダルで予定一覧を確認できる。
- 自動化: `e2e/tests/responsive.spec.ts`

<a id="e2e-resp-005"></a>
## E2E-RESP-005 主要画面は320pxから430pxで横にはみ出さない

- 対応元: 全画面スマートフォン表示改善。
- 前提データ: デモデータ。
- 手順: 320px、390px、430pxでホーム、家計簿、スケジュール、習慣、メモ、買い物、一時タスクを表示して最下部まで移動する。
- 期待値: ページ全体の横スクロールがなく、最終コンテンツが固定下部ナビに覆われない。
- 自動化: `e2e/tests/responsive.spec.ts`
- 手動確認: iOS SafariとAndroid Chromeのsafe-area、設定・認証・料理記録・管理画面、モーダル内スクロール。

<a id="e2e-ui-001"></a>
## E2E-UI-001 待ち時間のある画面遷移だけスケルトンを表示する

- 対応元: `task.md` スケルトンスクリーン
- 前提データ: なし。
- 手順: Service Workerを無効にして応答を400ms遅らせ、ホームから家計簿へ遷移する。離脱時点の表示を `pagehide` で記録する。
- 期待値: 180msの表示待ち後にスケルトンが表示され、遷移完了後には表示されていない。
- 自動化: `e2e/tests/skeleton.spec.ts`
- 手動確認: 低速回線での見え方、戻るボタン（bfcache）復元後にスケルトンが残らないこと。

<a id="e2e-undo-001"></a>
## E2E-UNDO-001 一時タスク削除がトーストで取り消せる

- 対応元: スマホの一時タスク誤削除対策（Undoトースト）
- 前提データ: なし。
- 手順: 一時タスクボードでカードを追加し、削除（`window.deleteTask`）→ Undoトーストの「元に戻す」を押す。
- 期待値: 削除でカードが消えてトーストが出る。「元に戻す」で復元され、リロード後もサーバーに残る。
- 自動化: `e2e/tests/undo-delete.spec.ts`
- 手動確認: 長押し削除・ドラッグ削除の両経路、複数削除時のトースト重なり。

<a id="e2e-undo-002"></a>
## E2E-UNDO-002 取り消さなければ削除が確定する

- 対応元: スマホの一時タスク誤削除対策（Undoトースト）
- 前提データ: なし。
- 手順: カードを追加し削除する。「元に戻す」を押さずにリロードする。
- 期待値: リロード後もカードは消えたまま（サーバー側でも削除済み）。
- 自動化: `e2e/tests/undo-delete.spec.ts`

<a id="e2e-budget-001"></a>
## E2E-BUDGET-001 全体予算とカテゴリ予算を設定して消化状況を確認できる

- 対応元: 新機能「予算管理」
- 前提データ: なし（テスト内でカテゴリを1件作成）。
- 手順: 家計簿設定でカテゴリを追加し、`/carbohydratepro/budget/` で全体予算とカテゴリ予算を設定する。
- 期待値: 全体予算の進捗バーが表示され、カテゴリ行に予算額と進捗バー要素が出る。ダッシュボードにも「今月の予算」バーが表示される。
- 自動化: `e2e/tests/budget.spec.ts`
- 手動確認: 超過時の赤バー・「超過」表示、警告(80%)の黄バー、デモではフォーム送信がサインアップモーダルでブロックされること。

<a id="e2e-cooking-001"></a>
## E2E-COOKING-001 手順付き料理を登録し今日の作成を記録できる

- 対応元: `workspace/cooking.md` 料理記録画面。
- 前提データ: `E2E_USER_EMAIL` と `E2E_USER_PASSWORD` で指定した有効ユーザーが存在する。
- 手順: 料理登録画面で料理名・材料を入力し、「手順ごと」を選んで2手順を登録する。詳細画面で次ページを開き、「今日作った」を押す。一覧で料理名を検索する。
- 期待値: 2つの手順がページとして表示され、次へボタンで切り替えられる。今日の記録後は累計作成回数が1回になり、料理名検索で対象が表示される。
- 自動化: `e2e/tests/cooking.spec.ts`
- 手動確認: 料理写真5枚、各手順写真2枚、左右スワイプ、390px幅、日別履歴の編集。

<a id="e2e-undo-003"></a>
## E2E-UNDO-003 全削除はアプリ内確認ダイアログで行う

- 対応元: `task.md` 一時タスクの全削除がブラウザ操作を固着させる不具合
- 前提データ: なし。
- 手順: 一時タスクボードでカードを追加し「全削除」を押す。アプリ内確認ダイアログのキャンセル／すべて削除を操作する。
- 期待値: `window.confirm` は発火せず、アプリ内ダイアログが出る。キャンセルで残り、確定で全て消える。
- 自動化: `e2e/tests/undo-delete.spec.ts`

<a id="e2e-undo-004"></a>
## E2E-UNDO-004 一時タスクの削除操作はaria-label付きボタン

- 対応元: `task.md` 一時タスクの削除アイコンがアクセシブルな操作対象になっていない
- 前提データ: なし。
- 手順: カードを追加し、削除要素を確認する。
- 期待値: 削除は `<button>` で `aria-label` を持つ。
- 自動化: `e2e/tests/undo-delete.spec.ts`

<a id="e2e-demo-002"></a>
## E2E-DEMO-002 デモの登録操作はサインアップモーダルを出しエラーを出さない

- 対応元: `task.md` デモ家計簿の登録ボタンが無反応に見える
- 前提データ: なし。
- 手順: `/demo/expenses/` で「収支登録」を押す。
- 期待値: デモ用サインアップモーダルが表示され、エラーalert・コンソールエラーは出ない。
- 自動化: `e2e/tests/demo.spec.ts`

<a id="e2e-demo-003"></a>
## E2E-DEMO-003 デモ家計簿で比較・絞り込み・年表示を操作できる

- 対応元: 家計簿の先月・全期間平均比較と、実画面に対するデモ機能差の解消。
- 前提データ: なし。
- 手順: `/demo/expenses/` で月次比較を開き、カテゴリグラフから一覧を絞り込んだ後、年表示を開く。
- 期待値: 選択月・先月・記録月全体の平均が表示される。グラフとページを再読み込みせず一覧だけが更新され、年表示にも月別グラフが表示される。
- 自動化: `e2e/tests/demo.spec.ts`

<a id="e2e-bulk-001"></a>
## E2E-BULK-001 メモを選択モードでまとめて削除・取り消しできる

- 対応元: 各画面の一括削除（選択モード）
- 前提データ: なし（テスト内でメモを2件作成）。
- 手順: メモ一覧で「選択」→ 2件チェック → 「削除」→ Undoトーストの「元に戻す」。再度削除してリロード。
- 期待値: 選択件数が表示され、削除で2件が消えてトーストが出る。「元に戻す」で復活。取り消さなければリロード後もサーバーから消えている。
- 自動化: `e2e/tests/undo-delete.spec.ts`
- 手動確認: 買い物・家計簿でも同様に動くこと、デモではサインアップモーダルでブロックされること。

<a id="e2e-trash-001"></a>
## E2E-TRASH-001 削除したメモをごみ箱から復元できる

- 対応元: 誤削除に備えた直近10件の永続復元。
- 前提データ: なし（テスト内でメモを1件作成）。
- 手順: メモを削除し、ナビゲーションからごみ箱を開いて対象の「元に戻す」を押す。
- 期待値: ごみ箱に削除日時・種別・タイトルが表示され、復元後はごみ箱から消えてメモ一覧へ再表示される。
- 自動化: `e2e/tests/undo-delete.spec.ts`
- 手動確認: 家計簿・定期支払い・予定・一時タスク・買いもの・習慣でも同様に復元できること、11件目の削除で最古の履歴が消えること。

<a id="e2e-fixes-001"></a>
## E2E-FIXES-001 家計簿の詳細絞り込みと週の開始曜日を操作できる

- 対応元: `task.md` 機能修正（2026-07-26）。
- 前提データ: `E2E_USER_EMAIL` と `E2E_USER_PASSWORD` で指定した有効ユーザーが存在する。
- 手順: ホームの「今日のひとこと・天気」を展開して簡易情報を確認し、家計簿の検索パネルを開く。テスト用取引を登録してグラフから絞り込み、タスク設定で週の開始を月曜に保存した後、日曜に戻す。
- 期待値: 天気はボタン操作前に位置情報を要求せず、収入・支出・収支は中央揃えで表示される。グラフ操作ではページやグラフを再読み込みせず取引一覧だけが更新され、月曜開始が保存後も選択されている。
- 自動化: `e2e/tests/feature-fixes.spec.ts`
- 手動確認: グラフの各要素クリック後の絞り込み結果、実機の位置情報許可、7桁・8桁金額の見え方。

<a id="e2e-ui-002"></a>
## E2E-UI-002 ホームの補助情報は必要なときだけ展開できる

- 前提データ: デモデータ、390px幅。
- 手順: ホームで「今日のひとこと・天気」をキーボードで展開し、クリックで閉じる。
- 期待値: 初期状態では補助情報が閉じており、展開すると天気ボタンが表示される。横にはみ出さない。
- 自動化: `e2e/tests/design-system.spec.ts`

<a id="e2e-ui-003"></a>
## E2E-UI-003 PCの主要画面は共通レイアウトと先行CSSを使用する

- 前提データ: デモデータ、1440px幅。
- 手順: 家計簿、スケジュール、メモを表示する。
- 期待値: サイドバーが表示され、機能CSSはheadで一度だけ読み込まれる。横にはみ出さず、カレンダーは960pxを超える作業幅を使える。ログインのコンテナは800px以下で、一覧の拡幅に巻き込まれない。
- 自動化: `e2e/tests/design-system.spec.ts`

<a id="e2e-ui-004"></a>
## E2E-UI-004 家計簿は表示するグラフだけ生成し再展開で再生成しない

- 前提データ: 2026年3月のデモデータ。
- 手順: 390pxで初期表示、日別支出を展開・閉じる・再展開。1440pxへ切り替え、390pxへ戻して収支を展開する。
- 期待値: 初期生成はカテゴリ1個だけ。未表示グラフは表示時に生成し、生成済みインスタンスは保持する。折れ線はアニメーション待ちなしで全日分を描画する。
- 自動化: `e2e/tests/design-system.spec.ts`

<a id="e2e-ui-005"></a>
## E2E-UI-005 記録がない月の収支グラフも有限の目盛りで表示する

- 前提データ: 記録のない2027年1月のデモデータ。
- 手順: PC表示で日別収支グラフを確認する。
- 期待値: 31日分のゼロが描画され、目盛りがNaNやInfinityにならない。
- 自動化: `e2e/tests/design-system.spec.ts`

## 未自動化だが優先度が高い項目

以下は `test.md` 全体を Playwright に落とすための追加ケースです。各ケースはテスト名に ID を含め、対応 spec からこのファイルへコメントで紐づけます。

<a id="public-and-error-pages"></a>
## 公開ページ・エラーページ

- `E2E-PUBLIC-001`: `/top/` が表示できる。
- `E2E-PUBLIC-002`: `/login/` が表示できる。
- `E2E-PUBLIC-003`: `/signup/` が表示できる。
- `E2E-PUBLIC-004`: `/password_reset/` が表示できる。
- `E2E-PUBLIC-005`: `/demo/expenses/` が表示できる。
- `E2E-PUBLIC-006`: `robots.txt` と `sitemap.xml` が返る。
- `E2E-PUBLIC-007`: 存在しない URL で 404 になり、内部情報が表示されない。
- 自動化: `e2e/tests/public.spec.ts`
- 手動確認: 本番 `DEBUG=False` の 500 ページ、Safari/iOS Safari の表示。

<a id="security-and-access-control"></a>
## セキュリティ・アクセス制御

- `E2E-SEC-001`: 未認証で保護ページへアクセスすると `/login/?next=...` へ遷移する。
- `E2E-SEC-002`: XSS 風のメモ本文を保存しても script が実行されない。
- 対象保護ページ: 家計簿、家計簿設定、定期支払い、タスク、タスク設定、一時タスク、習慣、習慣一覧、メモ、メモ設定、買い物、お問い合わせ、マイページ、プロフィール編集、パスワード変更。
- 自動化: `e2e/tests/access-control.spec.ts`, `e2e/tests/security-operations.spec.ts`
- 手動確認: 他ユーザー ID 指定の 403/404、CSRF トークン欠落時の挙動、パスワード/トークンのログ露出。

<a id="authenticated-pages"></a>
## 認証後ページ表示

- `E2E-HOME-001`: 統合ダッシュボード（ホーム）が表示できる。今日のタスク・今日の習慣・今月の収支・買い物リスト・最近のメモを1画面で確認できる。
- `E2E-EXPENSES-001`: 家計簿一覧が表示できる。
- `E2E-EXPENSES-SET-001`: 家計簿設定が表示できる。
- `E2E-RECURRING-001`: 定期支払い一覧が表示できる。
- `E2E-TASK-001`: スケジュールが表示できる。
- `E2E-TASK-SET-001`: タスク設定が表示できる。
- `E2E-BOARD-001`: 一時タスクボードが表示できる。
- `E2E-HABIT-001`: 習慣ダッシュボードが表示できる。
- `E2E-HABIT-LIST-001`: 習慣一覧が表示できる。
- `E2E-MEMO-001`: メモ一覧が表示できる。
- `E2E-MEMO-SET-001`: メモ種別設定が表示できる。
- `E2E-SHOPPING-001`: 買い物リストが表示できる。
- `E2E-CONTACT-001`: お問い合わせが表示できる。
- 自動化: `e2e/tests/authenticated-pages.spec.ts`
- 手動確認: 現在ページの視覚強調、空データ/大量データごとの細かい見た目。

<a id="filters-and-search"></a>
## 検索・絞り込み

- `E2E-FILTER-001`: 家計簿、メモ、買い物リストの検索・表示条件が URL に反映され、ページが表示できる。
- 自動化: `e2e/tests/authenticated-pages.spec.ts`
- 手動確認: 条件ごとの集計値、グラフ値、ページネーション境界。

<a id="crud-and-api"></a>
## 主要 CRUD・API

- 前提データ: `seed_e2e_user` で作成したテストユーザー、支払方法、カテゴリ、メモ種別が存在する。
- 共通期待値: 作成した `e2e-*` データが一覧に表示され、テスト内で削除される。テスト用ユーザー以外の既存データには依存しない。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| `E2E-EXPENSES-CRUD-001` | 家計簿一覧で収支登録モーダルを開き、日付、金額、用途、取引タイプ、費用タイプ、カテゴリ、支払方法を入力して登録し、検索して削除する。 | 登録後に検索結果へ用途が表示され、削除後は一覧から消える。 |
| `E2E-RECURRING-CRUD-001` | 定期支払い新規登録画面で用途、金額、分類、支払方法、頻度を入力して登録し、有効状態を切り替えて削除する。 | 登録後に定期支払い一覧へ用途が表示され、切替POSTが成功し、削除後は一覧から消える。 |
| `E2E-MEMO-CRUD-001` | メモ作成モーダルでタイトル、種別、本文、お気に入りを入力して登録し、検索、お気に入り切替、削除を行う。 | 検索結果へタイトルが表示され、切替POSTが成功し、削除後は一覧から消える。 |
| `E2E-SHOPPING-CRUD-001` | 買い物追加モーダルで一時買い物を登録し、検索、購入済み切替、削除を行う。 | 検索結果へタイトルが表示され、購入済み切替POSTが成功し、削除後は一覧から消える。 |
| `E2E-TASK-CRUD-001` | タスク登録モーダルで終日タスクを登録し、日表示へ移動して削除する。 | 対象日のガント表示にタイトルが表示され、削除POSTが成功する。 |
| `E2E-HABIT-CRUD-001` | 習慣一覧で習慣追加モーダルを開き、毎日の良い習慣を登録し、ダッシュボードで達成切替して削除する。 | 習慣一覧にタイトルが表示され、達成切替POSTが成功し、削除後は一覧から消える。 |
| `E2E-BOARD-CRUD-001` | 一時タスクボードAPIでセット作成、カード作成、カード更新、カード削除、セット削除を行う。 | 各APIが成功ステータスを返し、作成したセットとカードを削除できる。 |

- 自動化: `e2e/tests/crud.spec.ts`
- 手動確認: ダブルクリック/ダブルタップ編集、長押し削除、ドラッグ移動、細かいモーダル操作。

<a id="settings-and-contact"></a>
## 設定・お問い合わせ

- 前提データ: `E2E_USER_EMAIL` と `E2E_USER_PASSWORD` で指定した有効ユーザーが存在する。

| ID | 手順 | 期待値 |
| --- | --- | --- |
| `E2E-SETTINGS-001` | 家計簿設定で支払方法とカテゴリを追加し、一覧表示を確認して削除する。 | 追加した支払方法とカテゴリが表示され、削除後は表示されない。 |
| `E2E-SETTINGS-002` | タスク設定でラベル作成モーダルを開き、ラベル名と色を入力して作成し、削除する。 | 作成したラベルが表示され、削除後は表示されない。 |
| `E2E-SETTINGS-003` | メモ種別設定で種別作成モーダルを開き、名称と色を入力して作成し、削除する。 | 作成した種別が表示され、削除後は表示されない。 |
| `E2E-CONTACT-CRUD-001` | お問い合わせ画面で種類、件名、本文を入力して送信する。 | 送信後にお問い合わせ履歴へ件名が表示される。 |

- 自動化: `e2e/tests/settings-contact.spec.ts`
- 手動確認: 編集/削除、利用中マスタ削除時の表示、メール送信先と管理側対応。

<a id="operations-and-data-integrity"></a>
## 運用・データ整合性

- `E2E-OPS-001`: 主要一覧を表示、再読み込みしても破綻しない。
- `E2E-OPS-002`: ログアウト後にブラウザバックしても保護ページが利用できない。
- 自動化: `e2e/tests/security-operations.spec.ts`
- 手動確認: サーバーログ、ネットワーク遅延、二重送信、cron による定期支払い、月末/年末/うるう年。

<a id="e2e-memo-ui-001"></a>
## E2E-MEMO-UI-001 メモ画面の操作試作

- 一時ユーザーで登録・保存中の無効化・サーバー入力エラー・入力保持・保存トーストを確認する。
- 本文をHTMLとして実行しない。お気に入りの切替は通常一覧を再読み込みしない。
- 「…」から編集と削除確認を開ける。削除完了は画面を覆わない通知とし、ごみ箱から復元できる旨を表示する。
- 自動化: `e2e/tests/memo-prototype.spec.ts`。ローカル一時ユーザーのE2Eメモ種別を使用。

## 自動化しない/別系統で確認する項目

- 端末依存: iOS Safari、Android Chrome 実機、タッチ長押し/スワイプの体感。
- 目視品質: 色、余白、固定フッター被り、細かい崩れ、グラフの見やすさ。
- 運用環境: 本番 `DEBUG=False` の 500 ページ、サーバーログ、メール配送、cron 実行。
- 高負荷: 大量データ性能、キャッシュ、ヒートマップ大量 DOM。

<a id="e2e-ui-006"></a>
## E2E-UI-006 復帰時と同一画面リンクの待ち表示

- 即座にスケルトンを出さず、pageshowで予約も取り消す。同一画面内アンカーでは出さない。
- 自動化: `e2e/tests/skeleton.spec.ts`

<a id="e2e-ui-007"></a>
## E2E-UI-007 家計簿のボタン絞り込み・解除

- キーボードでカテゴリを選択し、解除する。リストだけ更新し、期間・canvas・Chartインスタンスを維持する。
- 自動化: `e2e/tests/design-system.spec.ts`

<a id="e2e-ui-008"></a>
## E2E-UI-008 習慣のキーボード操作

- 矢印・Homeキーで期間タブを切り替え、非選択パネルを操作対象外にする。Enterで習慣の達成・戻すを操作し、サーバー結果を反映する。
- 自動化: `e2e/tests/design-system.spec.ts`。ローカル一時ユーザーと「E2E毎日の習慣」を使用。

<a id="e2e-ui-009"></a>
## E2E-UI-009 認証フォームとスキップリンク

- 320・390・1440pxで入力幅・横はみ出しを確認。最初のTabから本文へ移動できる。ログインボタンはfont-weight 500。共通の標準文字サイズ16.5pxはE2E-UI-002でも確認。
- 自動化: `e2e/tests/design-system.spec.ts`

<a id="e2e-ui-010"></a>
## E2E-UI-010 静的資産の配信

- ハッシュ付きChart.jsはgzipとimmutable、非ハッシュURLはno-cache。404を長期キャッシュしない。
- 自動化: `e2e/tests/asset-delivery.spec.ts`。Nginx経由のローカル環境を使用。

<a id="e2e-ui-011"></a>
## E2E-UI-011 オフラインとキャッシュ更新

- 旧アプリキャッシュを削除するが無関係なキャッシュは維持する。オフライン案内だけを保存し、通信断のページ遷移で表示する。
- 自動化: `e2e/tests/asset-delivery.spec.ts`

<a id="e2e-ui-012"></a>
## E2E-UI-012 設定・料理フォームのスマホ操作

- 320・390・430pxで横はみ出しと操作ラベルを確認。メモ種別・支払方法の編集ボタンからモーダルを開ける。
- 自動化: `e2e/tests/authenticated-pages.spec.ts`。ローカル一時ユーザーと専用マスタを使用。

<a id="e2e-ui-013"></a>
## E2E-UI-013 共通ボタンの寸法

- 家計簿・スケジュール・買い物・メモの主要操作がPCで36px、タッチ条件で40px、角丸8pxで一致し、横にはみ出さない。
- 自動化: `e2e/tests/design-system.spec.ts`。PCとhasTouch条件で実行する。

<a id="e2e-ui-014"></a>
## E2E-UI-014 認証・補助画面の共通UI

- 公開・認証・アカウント・ごみ箱・予算・問い合わせ・習慣管理で共通部品CSSをheadに読み込み、PCとスマホで横にはみ出さない。
- 自動化: `e2e/tests/unified-ui.spec.ts`。POST送信やメール送信は行わない。

<a id="e2e-ui-015"></a>
## E2E-UI-015 確認ダイアログのフォーカス

- 共通確認ダイアログを開くとキャンセルにフォーカスし、Tab/Shift+Tabはダイアログ内を循環する。Escapeで閉じると呼出元へ戻る。
- 自動化: `e2e/tests/unified-ui.spec.ts`。確認処理は何も書き込まないテスト用コールバックを使用する。
